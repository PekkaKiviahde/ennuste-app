{
  "name": "ennuste-app-mvp",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:start": "docker compose up -d",
    "db:status": "node tools/scripts/db-status.mjs",
    "db:migrate": "node tools/scripts/db-migrate.mjs",
    "db:seed-demo": "node tools/scripts/db-seed-demo.mjs",
    "dev": "npm --workspace apps/web run dev",
    "agent:workflow-report": "sh tools/scripts/agent_workflow_report_pr.sh",
    "ci:guards": "bash tools/ci/guard_no_openai_in_web.sh",
    "lint": "npm run typecheck",
    "test": "npm --workspace packages/domain run test && npm --workspace packages/application run test && npm --workspace packages/infrastructure run test",
    "typecheck": "tsc -p packages/domain/tsconfig.json && tsc -p packages/application/tsconfig.json && tsc -p packages/infrastructure/tsconfig.json && tsc -p packages/shared/tsconfig.json && tsc -p apps/web/tsconfig.json"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
