{
  "name": "ennuste-app-api",
  "version": "0.1.0",
  "private": true,
  "description": "MVP API for Ennuste app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:migrate": "node scripts/migrate.js",
    "db:seed": "node scripts/seed.js",
    "db:setup": "npm run db:migrate && npm run db:seed",
    "smoke:api": "node scripts/smoke-api.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}
